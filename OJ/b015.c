#include<stdio.h>
int main(){
    unsigned int n;
    scanf("%d", &n);
    unsigned short A,B;
    scanf("%d%d", &A, &B);
    if(( A >= 1 && A <=36) && (B>=1 && B <=36)){
    unsigned int s = 0;
    for(int i = 1 ; i <= n ; i++){
        if( )
        s = s + i;
    }
    printf("%d", s);
}}